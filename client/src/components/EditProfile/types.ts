export interface EditProfileFormData {
  username: string;
  email: string;
  bio: string;
  currentPassword: string;
  newPassword: string;
}

export interface OriginalData {
  email: string;
  pass: string;
}